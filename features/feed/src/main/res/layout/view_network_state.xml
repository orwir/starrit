<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="orwir.starrit.feature.feed.internal.adapter.NetworkStateViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="@dimen/space"
        android:paddingEnd="@dimen/space">

        <ProgressBar
            android:id="@+id/loading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:visibleOrGone="@{viewModel.loading}" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/error"
            style="@style/TextAppearance.MyTheme.Subtitle1"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@{viewModel.errorMessage}"
            app:layout_constraintBaseline_toBaselineOf="@id/retry"
            app:layout_constraintEnd_toStartOf="@id/retry"
            app:layout_constraintStart_toStartOf="parent"
            app:visibleOrGone="@{viewModel.failed}"
            tools:text="Loading error" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/retry"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="@{viewModel.retry}"
            android:text="@string/network_state_button_retry"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/error"
            app:layout_constraintTop_toTopOf="parent"
            app:visibleOrGone="@{viewModel.failed}" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>